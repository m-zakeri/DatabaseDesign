import React from "react";
import LoginForm from "../components/LoginForm";

const page = () => {
  return (
    <main className="text-main-dark bg-main-grey h-screen flex justify-center items-center">
      <LoginForm />
    </main>
  );
};

export default page;
